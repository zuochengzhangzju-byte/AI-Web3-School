---
timezone: UTC+8
---

# wbc351

**GitHub ID:** wbc351

**Telegram:** 

## Self-introduction

AI x Web3 School

## Notes

<!-- Content_START -->
# 2026-05-20
<!-- DAILY_CHECKIN_2026-05-20_START -->
11 在动手之前，先搞清楚为什么要用 Hermes：

12

13 | 特性 | 说明

|

14 | -------------- | -----------------------------------------------------------

\- |

15 | \*\*启动速度快\*\* | Hermes 采用预编译字节码（Ahead-of-Time, AOT）的方式，省去了

运行时解析和编译 JS 的过程 |

16 | \*\*内存占用低\*\* | 针对移动端优化，堆内存管理更精细，适合小程序的内存受限场景

|

17 | \*\*包体积更小\*\* | 字节码比源码更紧凑，有助于控制小程序包体积

|

18 | \*\*首屏更快\*\* | 减少了冷启动阶段的 JS 解析时间，首屏渲染明显加速

|

19

20 ---

21

22 ## 二、前提条件

23

24 在配置 Hermes 之前，请确认以下条件：

25

26 1. \*\*微信开发者工具版本\*\* >= `1.06.2209190`（建议使用最新稳定版）

27 2. \*\*基础库版本\*\* >= `2.27.1`（在 `app.json` 或项目设置中指定）

28 3. 项目使用的是 \*\*微信小程序原生框架\*\* 或 \*\*支持 Hermes 的第三方框架\*\*（如 Tar

o 3.6+、uni-app 部分版本）

29 4. 确保项目中没有依赖 Hermes 不支持的 JS 语法特性（详见后文兼容性说明）

30

31 ---

32

33 ## 三、配置步骤

34

35 ### 步骤 1：升级微信开发者工具

36

37 前往 \[微信开发者工具官网\]([https://developers.weixin.qq.com/miniprogram/dev/dev](https://developers.weixin.qq.com/miniprogram/dev/dev)

tools/download.html) 下载最新版本。安装后打开项目，确认版本号满足要求。

38

39 ### 步骤 2：在 `project.config.json` 中启用 Hermes

40

41 在项目根目录的 `project.config.json` 文件中，找到或新增 `setting` 字段，添加以

下配置：

42

43 \`\`\`json

44 {

45 "setting": {

46 "compileHotReLoad": true,

47 "useIsolateContext": true,

48 "useCompilerModule": true,

49 "userConfirmedUseCompilerModuleSwitch": false

50 },

51 "compileType": "miniprogram",

52 "condition": {}

53 }

54 \`\`\`

55

56 关键字段说明：

57

58 - `useIsolateContext`：启用隔离上下文，Hermes 模式下建议开启

59 - `useCompilerModule`：启用编译模块，支持更细粒度的编译控制

60

61 ### 步骤 3：在 `app.json` 中指定基础库版本

62

63 确保 `app.json` 中设置了足够高的最低基础库版本：

64

65 \`\`\`json

66 {

67 "miniprogramRoot": "./",

68 "setting": {

69 "es6": true,

70 "postcss": true,

71 "minified": true

72 },

73 "libVersion": "2.27.1"

74 }

75 \`\`\`

76

77 ### 步骤 4：使用"增强编译"模式

78

79 Hermes 引擎通常与"增强编译"配合使用。在微信开发者工具中：

80

81 1. 点击右上角 \*\*详情\*\* -> \*\*本地设置\*\*

82 2. 勾选 \*\*使用增强编译\*\*

83 3. 或者在 `project.config.json` 中设置：

84

85 \`\`\`json

86 {

87 "setting": {

88 "enhance": true,

89 "es6": true

90 }

91 }

92 \`\`\`

93

94 增强编译会将 ES6+ 语法转译为 ES5，同时保留 Hermes 所需的优化信息。

95

96 ### 步骤 5：清理缓存并重新编译

97

98 配置完成后，执行以下操作：

99

100 1. 在开发者工具中点击 \*\*清缓存\*\* -> \*\*全部清除\*\*

101 2. 重新编译项目

102 3. 观察控制台是否有 Hermes 相关的日志输出，确认引擎已切换

103

104 ### 步骤 6：真机调试验证

105

106 Hermes 的优势主要体现在真机上，模拟器差异不大：

107

108 1. 点击 \*\*预览\*\* 或 \*\*真机调试\*\*

109 2. 在真机上观察启动速度和页面渲染

110 3. 使用 \*\*性能面板\*\* 查看 FPS、内存、JS 执行时间等指标

111

112 ---

113

114 ## 四、兼容性注意事项

115

116 Hermes 对 JavaScript 标准的支持与 V8 不完全一致，以下是一些需要注意的点：

117

118 ### 1. 不支持 `eval()` 和 `new Function()`

119

120 Hermes 出于安全和性能考虑，禁用了动态代码执行：

121

122 \`\`\`javascript

123 // ❌ 不支持

124 eval('console.log("hello")');

125 new Function('return 1')();

126

127 // ✅ 替代方案：使用静态逻辑替代动态执行

128 \`\`\`

129

130 ### 2. 部分 ES2020+ 特性需确认支持

131

132 - `Optional chaining?.`）：支持

133 - `Nullish coalescing??`）：支持

134 - `Promise.allSettled`：支持

135 - `Top-level await`：\*\*不支持\*\*（需包在 async 函数中）

136

137 ### 3. `Proxy` 和 `Reflect` 的限制

138

139 Hermes 对 `Proxy` 的支持有限，某些 trap 可能不完全兼容：

140

141 \`\`\`javascript

142 // ⚠️ 可能有问题的用法

143 const proxy = new Proxy(target, {

144 apply: function(target, thisArg, args) {

145 // 某些 trap 行为可能与 V8 不一致

146 }

147 });

148 \`\`\`

149

150 ### 4. `Symbol` 的部分特性

151

152 `Symbol.toPrimitiveSymbol.species` 等内置 Symbol 的支持程度需要逐个测试。

153

154 ---

155

156 ## 五、性能对比与调试技巧

157

158 ### 对比测试方法

159

160 在 `project.config.json` 中通过开关 Hermes，分别测试同一场景：

161

162 \`\`\`

163 Hermes 开启 vs 关闭 → 对比以下指标：

164 ├── 冷启动时间（从点击图标到首屏渲染完成）

165 ├── 首屏渲染时间（setData 到视图更新）

166 ├── JS 引擎初始化耗时

167 └── 内存峰值占用

168 \`\`\`

169

170 ### 调试技巧

171

172 1. \*\*使用 `console.time` / `console.timeEnd`\*\* 测量关键代码段的执行时间

173 2. \*\*开启性能面板\*\*，关注 "JS 脚本" 和 "渲染" 的耗时分布

174 3. \*\*使用真机调试\*\*，模拟器无法反映 Hermes 的真实性能

175 4. \*\*检查包体积变化\*\*，开启前后对比 `dist` 目录大小

176

177 ---

178

179 ## 六、实践心得

180

181 ### 心得 1：渐进式启用，不要一步到位

182

183 不要在主干分支直接开启 Hermes。建议：

184

185 1. 新建一个 `hermes-test` 分支

186 2. 开启 Hermes 后全面回归测试

187 3. 重点测试边界场景（复杂表单、长列表、动画页面）

188 4. 确认无误后再合并到主干

189

190 ### 心得 2：关注第三方库的兼容性

191

192 这是最容易踩坑的地方。很多 npm 包在设计时假设运行环境是 V8，可能会用到 Hermes

不支持的特性：

193

194 - `eval` / `new Function`（常见于模板引擎、序列化库）

195 - 完整的 `Proxy` 支持（常见于状态管理库）

196 - 某些 `Symbol` 特性（常见于响应式框架）

197

198 \*\*建议\*\*：逐步替换有兼容问题的库，优先选择微信小程序生态内的库。

199

200 ### 心得 3：Hermes 对大体积小程序效果更明显

201

202 如果你的小程序页面多、逻辑复杂、JS 包体积较大（> 500KB），Hermes 的 AOT 编译优

势会非常明显。反之，如果小程序本身很轻量，提升可能不太显著。

203

204 ### 心得 4：字节码不可跨平台

205

206 Hermes 的预编译字节码是平台相关的。这意味着：

207

208 - iOS 和 Android 需要分别编译

209 - 不同 Hermes 版本的字节码不通用

210 - 升级 Hermes 版本后需要重新编译

211

212 ### 心得 5：持续关注官方更新

213

214 Hermes 在微信小程序中的支持还在不断演进，建议：

215

216 - 关注 \[微信开放社区\]([https://developers.weixin.qq.com/community/](https://developers.weixin.qq.com/community/)) 的公告

217 - 定期升级开发者工具和基础库

218 - 参与社区讨论，分享遇到的问题和解决方案

219

220 ### 心得 6：性能优化不止于引擎

221

222 Hermes 是一个引擎层面的优化，但小程序性能是一个系统工程：

223

224 \`\`\`

225 性能优化 = 引擎优化 + 代码质量 + 渲染优化 + 网络优化

226 \`\`\`

227

228 不要把所有希望都寄托在 Hermes 上，同时要做好：

229

230 - 减少不必要的 `setData` 调用

231 - 使用分包加载降低首包体积

232 - 优化图片和静态资源

233 - 合理使用缓存策略

234

235 ---

234

235 ---

236

237 ## 七、常见问题 FAQ

238

239 \*\*Q1：开启 Hermes 后小程序白屏了怎么办？\*\*

240

241 A：检查是否有第三方库使用了 `eval` 或 `Proxy` 等不兼容的 API。可以逐个注释页面

和组件，定位问题模块。

242

243 \*\*Q2：Hermes 模式下如何调试？\*\*

244

245 A：微信开发者工具的调试器仍然可用，但部分高级功能（如 CPU Profile）可能受限。

建议结合 `console.log` 和性能面板进行调试。

246

247 \*\*Q3：开启 Hermes 后包体积变大了？\*\*

248

249 A：正常情况下字节码应该比源码小。如果变大，可能是编译配置问题，检查是否同时开

启了不必要的 polyfill。

250

251 \*\*Q4：Hermes 和 Skyline 渲染引擎可以同时使用吗？\*\*

252

253 A：可以。Skyline 是渲染引擎，Hermes 是 JS 引擎，两者互不冲突，可以组合使用以获

得最佳性能。

254

255 ---

256

257 ## 八、总结

258

259 | 维度 | 建议 |

260 | ---------- | ---------------------------------- |

261 | 是否启用 | 推荐启用，尤其是中大型小程序 |

262 | 启用时机 | 在开发阶段充分测试后再上线 |

263 | 最大收益 | 冷启动速度和首屏渲染 |

264 | 主要风险 | 第三方库兼容性问题 |

265 | 长期策略 | 持续关注官方更新，逐步适配 |

266

267 Hermes 的引入是微信小程序性能优化的重要一步。它不是一个"银弹"，但确实能带来可

观的性能提升。关键在于充分测试、逐步迁移、持续优化。

268

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/AI-Web3-School/main/assets/wbc351/images/2026-05-20-1779286650464-image.png)
<!-- DAILY_CHECKIN_2026-05-20_END -->

# 2026-05-19
<!-- DAILY_CHECKIN_2026-05-19_START -->


环境

WSL2 + Ubuntu，Hermes Agent 通过 pip 安装。初次启动后会在 ~/.hermes/ 下自动生成配置目录。

核心配置文件

┌───────────────────────┬────────────────────────────────────────────────────┐

│ 文件 │ 作用 │

├───────────────────────┼────────────────────────────────────────────────────┤

│ ~/.hermes/config.yaml │ 主配置，模型、provider、超时、压缩、显示等所有参数 │

├───────────────────────┼────────────────────────────────────────────────────┤

│ ~/.hermes/.env │ API Key 等敏感信息，和 config.yaml 配合使用 │

└───────────────────────┴────────────────────────────────────────────────────┘

config.yaml 内容很多，但大部分默认值不用动，主要关注顶部的 model 段落就行。

模型切换

默认用的是 DeepSeek，想换成别的模型（比如小米 MiMo 代理），改三个地方就行：

config.yaml 顶部：

model:

default: mimo-v2.5-pro

provider: openai

base\_url: [https://token-plan-sgp.xiaomimimo.com/anthropic](https://token-plan-sgp.xiaomimimo.com/anthropic)

.env 里换成对应的 key：

OPENAI\_API\_KEY=你的key

OPENAI\_BASE\_URL=[https://token-plan-sgp.xiaomimimo.com/anthropic](https://token-plan-sgp.xiaomimimo.com/anthropic)

provider 选 openai 就行，因为 MiMo 代理走的是 OpenAI

兼容接口。大多数第三方代理服务都支持这个格式，所以换模型基本就是换个 base\_url 和 API Key 的事。

其他实用配置

\- max\_turns（agent 段）：单次对话最大轮数，默认 90，够用了。

\- compression：上下文压缩，开了之后长对话不容易爆 context，建议保持默认开启。

\- display.language：改成 zh 可以让界面显示中文。

\- terminal.timeout：命令执行超时，默认 180 秒，跑长脚本可以调大。

\- fallback\_model：配置备用模型，主模型挂了自动切换，config.yaml 底部有注释示例。

踩的坑

\- 改完配置要重启 Hermes 才生效，光改文件没用。

\- .env 里的变量名要和 provider 对应，用 openai provider 就得是 OPENAI\_API\_KEY，写成 DEEPSEEK\_API\_KEY 会读不到。

\- Hermes 的 provider 是 openai 而不是 anthropic，即使底层模型是 Claude，走 OpenAI 兼容接口就选 openai。

\- config.yaml 里有个 _config_version 字段，别手动改，Hermes 升级时会自动处理。

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/AI-Web3-School/main/assets/wbc351/images/2026-05-19-1779199076753-image.png)
<!-- DAILY_CHECKIN_2026-05-19_END -->

# 2026-05-18
<!-- DAILY_CHECKIN_2026-05-18_START -->




1 # Claude Code 配置指南

2

3 > **AI城 · Web3 黑客松训练营 · 学习笔记**

4 > 2026-05-18

5

6 ---

7

8 ## 一、什么是 Claude Code？

9

10 Claude Code 是 Anthropic 推出的\*\*终端 AI 编程助手\*\*，直接集成在命令行中。它可以：

11

12 - 理解并修改代码库

13 - 执行 Git 操作（commit、push、PR）

14 - 运行测试、调试代码

15 - 与 MCP 服务器集成，扩展能力（如链上交互、合约验证）

16

17 对于 Web3 开发者来说，Claude Code + MCP = **AI Agent 驱动的智能合约开发工作流**。

18

19 ---

20

21 ## 二、安装

22

23 ### 环境要求

24

25 - **Node.js** >= 18.x

26 - **npm** >= 9.x

27 - **Git** >= 2.20

28

29 ### 安装命令

30

31 \`\`\`bash

32 npm install -g @anthropic-ai/claude-code

33 \`\`\`

34

35 验证安装：

36

37 \`\`\`bash

38 claude --version

39 \`\`\`

40

41 ---

42

43 ## 三、国内环境配置（阿里云百炼方案）

44

45 由于 Claude API 在国内无法直接访问，推荐通过\*\*阿里云百炼\*\*接入。

46

47 ### 3.1 获取 API Key

48

49 1. 访问 \[阿里云百炼控制台\]([https://dashscope.aliyun.com/](https://dashscope.aliyun.com/))

50 2. 选择地域 **华北2（北京）**

51 3. 在「模型广场」开通所需模型（如 `qwen3.6-plus`）

52 4. 创建 API Key`sk-` 开头，\*\*仅显示一次，务必保存\*\*）

53

54 ### 3.2 跳过官方登录

55

56 创建 `~/.claude.json`（Windows: `C:\Users\<用户名>\.claude.json`）：

57

58 \`\`\`json

59 {

60 "hasCompletedOnboarding": true

61 }

62 \`\`\`

63

64 ### 3.3 配置 settings.json

65

66 创建或编辑 `~/.claude/settings.json`：

67

68 \`\`\`json

69 {

70 "autoUpdatesChannel": "stable",

71 "language": "chinese",

72 "showTurnDuration": true,

73 "env": {

74 "ANTHROPIC\_AUTH\_TOKEN": "sk-你的百煉APIKey",

75 "ANTHROPIC\_BASE\_URL": "[https://dashscope.aliyuncs.com/apps/anthropic](https://dashscope.aliyuncs.com/apps/anthropic)",

76 "ANTHROPIC\_MODEL": "qwen3.6-plus",

77 "ANTHROPIC\_DEFAULT\_HAIKU\_MODEL": "qwen3.6-plus",

78 "ANTHROPIC\_DEFAULT\_SONNET\_MODEL": "qwen3.6-plus",

79 "ANTHROPIC\_DEFAULT\_OPUS\_MODEL": "qwen3.6-plus",

80 "CLAUDE\_CODE\_SUBAGENT\_MODEL": "qwen3.6-plus"

81 }

82 }

83 \`\`\`

84

85 > **注意**：按量计费使用 `dashscope.aliyuncs.com` 域名；Coding Plan 用户需使用 \`coding-intl.dashscope.aliyuncs.

com\`。

86

87 ### 3.4 验证配置

88

89 \`\`\`bash

90 claude "你好"

91 \`\`\`

92

93 能看到模型正常响应即配置成功。

94

95 ---

96

97 ## 四、配置文件体系

98

99 Claude Code 采用\*\*多层配置覆盖\*\*机制：

100

101 \`\`\`

102 ~/.claude/

103 ├── settings.json # 全局配置（用户级，不提交）

104 └── [CLAUDE.md](http://CLAUDE.md) # 全局指令

105

106 my-web3-project/

107 ├── .claude/

108 │ ├── settings.json # 项目共享配置（可提交 Git）

109 │ └── settings.local.json # 项目本地配置（自动 gitignore，不提交）

110 ├── [CLAUDE.md](http://CLAUDE.md) # 项目指令

111 └── .mcp.json # MCP 服务注册表

112 \`\`\`

113

114 ### 优先级（从高到低）

115

116 1. `.claude/settings.local.json` — 个人覆盖（密钥、实验性配置）

117 2. `.claude/settings.json` — 项目共享

118 3. `~/.claude/settings.json` — 全局用户

119

120 ---

121

122 ## 五、核心配置详解

123

124 ### 5.1 基础配置

125

126 \`\`\`json

127 {

128 "language": "chinese",

129 "model": "sonnet",

130 "cleanupPeriodDays": 14,

131 "spinnerTipsEnabled": false,

132 "permissions": {

133 "defaultMode": "auto"

134 }

135 }

136 \`\`\`

137

138 | 字段 | 说明 | 推荐值 |

139 |------|------|--------|

140 | `language` | 语言 | `"chinese"` |

141 | `model` | 默认模型 | `"sonnet"` |

142 | `cleanupPeriodDays` | 自动清理旧会话的天数 | `14` |

143 | `permissions.defaultMode` | 权限模式（auto 自动放行安全操作） | `"auto"` |

144

145 ### 5.2 权限配置（Permissions）

146

147 精细化控制工具权限：

148

149 \`\`\`json

150 {

151 "permissions": {

152 "allow": \[

153 "Bash(npm \*)",

154 "Bash(pnpm \*)",

155 "Bash(node \*)",

156 "Bash(solana \*)",

157 "Bash(anchor \*)"

158 \],

159 "deny": \[

160 "Read(.env)",

161 "Read(.env.\*)",

162 "Bash(rm -rf \*)"

163 \]

164 }

165 }

166 \`\`\`

167

168 ### 5.3 环境变量

169

170 \`\`\`json

171 {

172 "env": {

173 "EDITOR": "code --wait",

174 "VISUAL": "code --wait",

175 "SOLANA\_CLUSTER": "devnet",

176 "ANCHOR\_WALLET": "~/.config/solana/id.json"

177 }

178 }

179 \`\`\`

180

181 对 Web3 项目，可以预设常用的 Solana/Anchor 环境变量。

182

183 ---

184

185 ## 六、Hooks（钩子）自动化

186

187 Hooks 可以在特定事件发生时自动执行命令，是实现\*\*自动化工作流\*\*的关键。

188

189 ### 6.1 支持的事件（部分）

190

191 | 事件 | 触发时机 |

192 |------|---------|

193 | `SessionStart` | 会话开始或恢复 |

194 | `UserPromptSubmit` | 用户提交消息后 |

195 | `PreToolUse` | 工具执行前（可阻止） |

196 | `PostToolUse` | 工具成功执行后 |

197 | `PostToolUseFailure` | 工具执行失败后 |

198 | `Stop` | Claude 完成响应 |

199

200 ### 6.2 实用示例

201

202 **编辑后自动格式化代码：**

203

204 \`\`\`json

205 {

206 "hooks": {

207 "PostToolUse": \[{

208 "matcher": "Edit|Write",

209 "hooks": \[{

210 "type": "command",

211 "command": "npx prettier --write \\"$CLAUDE\_PROJECT\_DIR/\*\*/\*.{ts,rs,js,json}\\"",

212 "timeout": 30

213 }\]

214 }\]

215 }

216 }

217 \`\`\`

218

219 **每次提交前运行测试：**

220

221 \`\`\`json

222 {

223 "hooks": {

224 "PreToolUse": \[{

225 "matcher": "Bash(git commit \*)",

226 "hooks": \[{

227 "type": "command",

228 "command": "cargo test 2>&1 || anchor test 2>&1",

229 "timeout": 120

230 }\]

231 }\]

232 }

233 }

234 \`\`\`

235

236 **桌面通知（Claude 需要输入时）：**

237

238 \`\`\`json

239 {

240 "hooks": {

241 "Notification": \[{

242 "hooks": \[{

243 "type": "command",

244 "command": "osascript -e 'display notification \\"Claude Code 需要你\\" with title \\"Claude Code\\"'"

245 }\]

246 }\]

247 }

248 }

249 \`\`\`

250

251 ### 6.3 5 种 Hook 类型

252

253 | 类型 | 说明 | 适用场景 |

254 |------|------|---------|

255 | `command` | 执行 Shell 命令 | 格式化、lint、测试 |

256 | `http` | 发送 HTTP 请求（v2.1.63+） | 通知 Webhook |

257 | `mcp_tool` | 调用 MCP 工具（v2.1.118+） | 链上验证 |

258 | `prompt` | 单轮 LLM 评估 | 代码审查 |

259 | `agent` | 多轮 Agent 验证（实验性） | 复杂检查 |

260

261 ---

262

263 ## 七、MCP 服务器配置

264

265 MCP（Model Context Protocol）是 Claude Code 的\*\*能力扩展层\*\*，对于 Web3 开发尤为关键。

266

267 ### 7.1 基本配置

268

269 `.mcp.json`（项目根目录）：

270

271 \`\`\`json

272 {

273 "mcpServers": {

274 "solana-tools": {

275 "command": "npx",

276 "args": \["@solana/web3.js-mcp-server"\]

277 },

278 "contract-verifier": {

279 "command": "python",

280 "args": \["path/to/[verifier-server.py](http://verifier-server.py)"\],

281 "env": {

282 "EXPLORER\_API": "[https://api.devnet.solana.com](https://api.devnet.solana.com)"

283 }

284 }

285 }

286 }

287 \`\`\`

288

289 ### 7.2 Web3 开发推荐 MCP 服务

290

291 | MCP 服务 | 用途 |

292 |----------|------|

293 | `solana-mcp-server` | Solana 链上交互、账户查询 |

294 | `anchor-mcp-server` | Anchor 框架合约部署验证 |

295 | `ipfs-mcp-server` | IPFS 文件存储与检索 |

296 | `explorer-mcp-server` | 区块链浏览器数据查询 |

297

298 ### 7.3 MCP + Hooks 联动

299

300 在 Hook 中直接调用 MCP 工具：

301

302 \`\`\`json

303 {

304 "hooks": {

305 "PostToolUse": \[{

306 "matcher": "Write(\*.sol)",

307 "hooks": \[{

308 "type": "mcp\_tool",

309 "server": "solana-tools",

310 "tool": "verify\_contract"

311 }\]

312 }\]

313 }

314 }

315 \`\`\`

316

317 > 这样每次写完 Solidity/Rust 合约代码，自动触发链上验证。

318

319 ---

320

321 ## 八、[CLAUDE.md](http://CLAUDE.md) 指令配置

322

323 `CLAUDE.md` 是 Claude 的项目级指令文件，告诉 Claude 项目的上下文和规则。

324

325 ### 项目级 [CLAUDE.md](http://CLAUDE.md) 示例（Web3 项目）

326

327 \`\`\`markdown

328 # Web3 项目规范

329

330 ## 技术栈

331 - 框架: Anchor 0.30

332 - 语言: Rust (合约), TypeScript (前端)

333 - 网络: Solana devnet

334

335 ## 目录结构

336 - `programs/` — 合约代码

337 - `tests/` — 测试

338 - `app/` — 前端

339

340 ## 开发流程

341 1. `anchor build` — 编译合约

342 2. `anchor test` — 运行测试

343 3. `anchor deploy` — 部署到 devnet

344

345 ## 规则

346 - 合约必须通过所有测试才能提交

347 - 使用 `solana config set --url devnet` 切换网络

348 - 私钥文件禁止提交到 Git

349 \`\`\`

350

351 ### 全局 [CLAUDE.md](http://CLAUDE.md) 示例

352

353 `~/.claude/CLAUDE.md`：

354

355 \`\`\`markdown

356 # 全局指令

357

358 - 默认使用中文回复

359 - 代码优先考虑可读性和安全性

360 - 对于 Web3 项目，始终检查 Gas 优化和安全漏洞

361 - 优先使用 Rust 和 TypeScript

362 \`\`\`

363

364 ---

365

366 ## 九、Web3 开发最佳配置

367

368 综合以上所有配置，一份面向 **Web3 黑客松** 的完整配置：

369

370 ### settings.json

371

372 \`\`\`json

373 {

374 "language": "chinese",

375 "model": "sonnet",

376 "permissions": {

377 "defaultMode": "auto",

378 "allow": \[

379 "Bash(solana \*)",

380 "Bash(anchor \*)",

381 "Bash(spl-token \*)",

382 "Bash(npm \*)",

383 "Bash(pnpm \*)",

384 "Bash(cargo \*)"

385 \]

386 },

387 "env": {

388 "SOLANA\_CLUSTER": "devnet",

389 "ANCHOR\_WALLET": "~/.config/solana/id.json",

390 "EDITOR": "code --wait"

391 },

392 "hooks": {

393 "PostToolUse": \[{

394 "matcher": "Edit|Write",

395 "hooks": \[{

396 "type": "command",

397 "command": "npx prettier --write \\"\*\*/\*.{ts,js,json,rs}\\" 2>/dev/null || true",

398 "timeout": 15

399 }\]

400 }\],

401 "PreToolUse": \[{

402 "matcher": "Bash(solana deploy \*)",

403 "hooks": \[{

404 "type": "command",

405 "command": "anchor build 2>&1",

406 "timeout": 120

407 }\]

408 }\]

409 }

410 }

411 \`\`\`

412

413 ### .mcp.json

414

415 \`\`\`json

416 {

417 "mcpServers": {

418 "solana": {

419 "command": "npx",

420 "args": \["@solana/web3.js-mcp-server"\]

421 }

422 }

423 }

424 \`\`\`

425

426 ---

427

428 ## 十、常见问题

429

430 ### Q1: 国内无法访问 Claude API？

431

432 使用阿里云百炼/百度千帆等国内服务商中转。参考第三节配置。

433

434 ### Q2: 权限弹窗太多？

435

436 设置 `"permissions": { "defaultMode": "auto" }` 自动放行安全操作，或用 `/fewer-permission-prompts` 命令优化。

437

438 ### Q3: Hooks 不生效？

439

440 - 检查 Hook 事件名是否\*\*区分大小写\*\*（如 `PostToolUse` 不是 `posttooluse`）

441 - Hook 默认超时 60 秒，长任务需设置 `timeout`

442 - 退出码 `2` 会阻止操作（PreToolUse 场景）

443

444 ### Q4: MCP 服务器连接失败？

445

446 - 确认 `command` 和 `args` 中的路径正确

447 - 检查环境变量是否通过 `env` 字段正确传递

448 - 运行 `claude doctor` 诊断

449

450 ### Q5: 如何在项目中共享配置？

451

452 将 `.claude/settings.jsonCLAUDE.md.mcp.json` 提交到 Git 仓库，队友 `git clone` 后开箱即用。

453

454

455

456 十一、总结

457

456 ## 十一、总结

457

458 | 组件 | 作用 | 是否提交 Git |

459 |------|------|-------------|

460 | `~/.claude/settings.json` | 全局配置（API Key 等） | 否 |

461 | `~/.claude/CLAUDE.md` | 全局指令 | 否 |

462 | `.claude/settings.json` | 项目共享配置 | **是** |

463 | `.claude/settings.local.json` | 个人覆盖配置 | 否 |

464 | `CLAUDE.md` | 项目指令 | **是** |

465 | `.mcp.json` | MCP 服务器注册 | **是** |

466

467 ### 速查命令

468

469 \`\`\`bash

470 claude --version # 检查版本

471 claude doctor # 诊断配置

472 claude "你好" # 验证连通性

473 claude /config # 交互式配置
<!-- DAILY_CHECKIN_2026-05-18_END -->
<!-- Content_END -->
